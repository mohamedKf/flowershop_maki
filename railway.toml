[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn flowershop.wsgi --log-file -"
healthcheckPath = "/api/shops/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
